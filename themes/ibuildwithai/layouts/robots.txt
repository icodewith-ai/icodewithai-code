{{- if eq hugo.Environment "prod" }}
# Production robots.txt for www.ibuildwith.ai
# Allow all search engines to crawl the site

User-agent: *
Allow: /

# Sitemap location
Sitemap: {{ .Site.BaseURL | strings.TrimSuffix "/" }}/sitemap.xml

# Specific crawler optimizations
User-agent: Googlebot
Allow: /

User-agent: Bingbot
Allow: /

# Block access to admin/private areas (if any exist in the future)
Disallow: /admin/
Disallow: /private/
Disallow: /.git/

# Allow access to all content types
Allow: /blog/
Allow: /podcast/
Allow: /events/
Allow: /apps/
Allow: /about-marcelo/
Allow: /contact/

{{- else }}
# {{ if eq .Site.BaseURL "https://next.ibuildwith.ai" }}Staging{{ else if hasPrefix .Site.BaseURL "http://localhost" }}Development{{ else }}Non-production{{ end }} robots.txt
# Disallow all search engine crawling for non-production environments

User-agent: *
Disallow: /

# No sitemap for non-production environments
{{- end }}